import React from "react";
import { HeaderDivider } from "./Header";


export const Footer: React.FC = () => {
    return (
        <HeaderDivider className="border-t-2 border-t-defaultGreen mt-7 text-white items-center justify-center font-montserrat font-bold text-center">
            That's All for Now
        </HeaderDivider>
    )
}